Lets now develop and deploy the Hope Turtles / Ecojoiner generator backend. The frontend form is already deployed at `/views/generate`; implement the backend that receives its bottle and wood fields, validates them, calculates derived Ecojoiner v3.2 dimensions, then generates downloadable files under `public/ecojoiner_exports/<jobSlug>/`. Use the naming pattern `ecojoiner_<brand>_<volume>L_<johnLength>x<johnHeight>_<jobId>`. The current design is v3.2: Long John x6, Little John x5, Master John x1, Final Key x4, and Presser x12; Saddlers have been removed. The form fields map to `slat_thickness`, `cap_diameter`, `collar_diameter`, `taper_height` or direct `port_length`, `port_height`/bottle diameter, `bottle_volume_l`, `bottle_brand`, and selected formats. Validation must happen server-side using these ranges: wood/slat thickness 3-30mm, port length 20-200mm, bottle diameter/port height 30-200mm, cap diameter 10-80mm, collar diameter 10-90mm, screw pilot diameter 3-12mm, fit clearance 0-2mm, and volume greater than 0L but no more than 5L. Also reject geometry where `port_height <= 2 * slat_thickness`, where cap/collar/screw holes are too large for the derived John height, or where less than 4mm of material remains above/below the cap or collar hole.

Place the provided Python generator script in the root `ecojoiner/generate_exports.py`, install `reportlab`, and call it from the backend with Node `execFile` or by importing/porting the same logic directly. Do not use shell interpolation. The script returns a JSON manifest containing `inputs`, `derived`, `files`, and public URLs; return this manifest to the frontend so the page can display derived dimensions and download links. The generated outputs are a Letter landscape carpenter reference PDF, a self-contained OpenSCAD `.scad` file, and 1:1 SVG cutting exports; future DXF/STL can be generated from the SCAD via OpenSCAD CLI, but keep this first implementation focused on PDF/SCAD/SVG. Ensure Express serves `public/ecojoiner_exports` statically at `/ecojoiner_exports`, creates one job folder per request, and never allows user input to write into the trusted root `ecojoiner/` source directory. Keep the frontend confirmation/preview flow: validate and show derived dimensions first, then generate selected downloads.

Please come up with a plan for implimentation!  I am looking forward to seeing what you got.  :-)